---
title: "Sample HTML Website"
description: Sample HTML website for testing
date: 2024-02-25
published: true
---

## Index.html

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>String Concatenation</title>
</head>
<body>
  <h1>Enter Two Strings</h1>
  <form action="result.html" method="GET">
    <label for="string1">String 1:</label>
    <input type="text" id="string1" name="string1" required><br><br>
    
    <label for="string2">String 2:</label>
    <input type="text" id="string2" name="string2" required><br><br>
    
    <button type="submit">Submit</button>
  </form>
</body>
</html>

```

## Result.html

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Concatenated Result</title>
</head>
<body>
  <h1>Concatenated Strings</h1>
  <p id="result"></p>

  <script>
    // Function to get query parameters
    function getQueryParam(param) {
      const urlParams = new URLSearchParams(window.location.search);
      return urlParams.get(param);
    }

    // Get the two strings from the URL query parameters
    const string1 = getQueryParam('string1');
    const string2 = getQueryParam('string2');

    // Concatenate the strings and display the result
    if (string1 && string2) {
      const concatenated = string1 + " " + string2;
      document.getElementById("result").textContent = "Concatenated result: " + concatenated;
    } else {
      document.getElementById("result").textContent = "Please enter valid strings.";
    }
  </script>
</body>
</html>

```